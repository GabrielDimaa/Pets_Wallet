import 'package:flutter/material.dart';

class PetScreen extends StatelessWidget {
	Map<String, dynamic> snapshot;

	PetScreen(this.snapshot);

	@override
	Widget build(BuildContext context) {
		return Container(
			color: Colors.red
		);
	}
}